t=(45,1,5458,4,1,15,185,48,5)
print(t)
# in sorted method tuple convert into list 
# so we use tuple function to convert list into tuple
print("sorted tuple is = ",sorted(t))
print("sorted tuple is = ",tuple(sorted(t)))